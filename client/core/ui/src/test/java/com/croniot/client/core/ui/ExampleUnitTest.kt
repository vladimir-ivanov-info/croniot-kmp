package com.croniot.client.core.ui

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class ExampleUnitTest {
    @Test
    fun `WHEN adding two and two THEN the result is four`() {
        assertEquals(4, 2 + 2)
    }
}
