package com.croniot.client.core.config

object AppConfig {
    val appName = "croniot"
    //var isDemo: Boolean = false // TODO variable will be inconsistent when app goes on background for too much time and user returns.
}
