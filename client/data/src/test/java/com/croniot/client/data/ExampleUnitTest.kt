package com.croniot.client.data

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class ExampleUnitTest {
    @Test
    fun `WHEN adding two numbers THEN the sum is correct`() {
        assertEquals(4, 2 + 2)
    }
}
